<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.SmartCampusCompanion" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:statusBarColor">@color/dark_green_brand</item>
        <!-- Customize your dark theme here. -->
        <item name="android:windowBackground">@color/custom_green_brand</item>
        <item name="colorPrimary">@color/light_green_brand</item>
    </style>
</resources>